import styled from '@emotion/styled';

export const PostViewButtons = styled.div`
    display: flex;
    gap: 4px;
    margin: 0 0 40px;
`;

export const PostViewRow = styled.dl`
    margin-bottom: 20px;
    padding: 0 18px;

    dt {
        margin-left: -8px;
        margin-bottom: 8px;
        color: var(--text-color-3);
        font-size: .9em;
    }
    dd {
        
    }
`;

export const PostViewFooter = styled.div`
    margin-top: 18px;
    text-align: center;
`;
